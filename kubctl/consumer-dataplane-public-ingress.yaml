apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ita-dataplane-public
  namespace: mvd
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/use-regex: "true"
    # Quitamos /ita-dp del path y dejamos /api/public...
    nginx.ingress.kubernetes.io/rewrite-target: /$2
spec:
  rules:
    - http:
        paths:
          - path: /ita-dp(/|$)(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: ita-dataplane
                port:
                  number: 11002